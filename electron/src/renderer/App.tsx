import React from 'react';

/**
 * Root application component for RT Flow.
 * Placeholder — replace with actual editor UI.
 */
const App: React.FC = () => {
  return (
    <div style={{ fontFamily: 'sans-serif', padding: '2rem' }}>
      <h1>RT Flow</h1>
      <p>Editor placeholder — implementation pending.</p>
    </div>
  );
};

export default App;
